[package]
name = "ir"
version = "0.1.0"
edition = "2021"
description = "Run standalone R scripts from embedded dependency metadata."
license = "MIT"

[[bin]]
name = "ir"
path = "src/main.rs"

[profile.release]
strip = true
