[package]
name = "ir"
version = "0.1.0"
edition = "2021"
description = "A uv-style front-end to R: run self-contained R scripts with inline dependencies."
license = "MIT"

[[bin]]
name = "ir"
path = "src/main.rs"

[profile.release]
strip = true
