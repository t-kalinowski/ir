---
title: "Reference"
execute:
  echo: false
  warning: false
  message: false
---

This page renders the current CLI help by running the `ir` executable on `PATH` during the Quarto render.

```{r}
cli_help <- function(...) {
  args <- c(..., "--help")
  output <- system2("ir", args, stdout = TRUE, stderr = TRUE)
  status <- attr(output, "status")

  if (!is.null(status)) {
    stop(
      "`ir ", paste(args, collapse = " "), "` failed with status ", status,
      call. = FALSE
    )
  }

  cat(output, sep = "\n")
}
```

## `ir`

```{r}
#| comment: ""
cli_help()
```

## `ir run`

```{r}
#| comment: ""
cli_help("run")
```

## `ir tool`

```{r}
#| comment: ""
cli_help("tool")
```

## `ir tool run`

```{r}
#| comment: ""
cli_help("tool", "run")
```

## `ir tool install`

```{r}
#| comment: ""
cli_help("tool", "install")
```

## `ir cache`

```{r}
#| comment: ""
cli_help("cache")
```

## `ir cache dir`

```{r}
#| comment: ""
cli_help("cache", "dir")
```

## `ir cache clean`

```{r}
#| comment: ""
cli_help("cache", "clean")
```
